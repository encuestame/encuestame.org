require(["jquery", "jquery.bootstrap", 'backbone'], function($, B, x) {
    //the jquery.alpha.js and jquery.beta.js plugins have been loaded.
    $(function() {
        console.log('HOMEEE', $);
        console.log('HOMEEE x', x);
    });
});
